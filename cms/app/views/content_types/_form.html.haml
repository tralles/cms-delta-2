= simple_form_for([@project, @content_type], html: {class: 'form-horizontal' }) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :intern
    = f.input :editor
    = f.input :meta
    
    = f.input :ref_id

  .form-actions
    = f.button :submit, :class => 'btn btn-primary', :data => { :disable_with => 'Übertrage Daten ...' }
