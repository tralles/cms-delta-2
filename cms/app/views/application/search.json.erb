[
  <% @contents.each do |content| %>
    {
      "title":"<%= content.rep @locale %>",
      "href":"<%= project_content_path(content.project, content) %>",
      "id":"<%= content.id %>"
    }<%= ', ' unless content == @contents.last %>
  <% end %>
]
