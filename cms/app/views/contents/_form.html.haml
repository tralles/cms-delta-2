.row
  .span6
    = simple_form_for([@project, @content_type, @content], html: {class: 'form-horizontal' }) do |f|
      = f.error_notification

      - if @locale
        = hidden_field_tag :locale, @locale
      

      %fieldset
        %legend
          Inhalte
            
        %options
          .form-inputs
            - @content_type.content_element_types.each do |cet|
              .control-group
                %label.control-label
                  = cet.name
                .controls
                  = cetform @content, cet
        
        
    
      %fieldset
        %legend
          Metadaten

            
        %options
          .form-inputs
            = f.input :ref_id
            = f.input :alpha_datum, :include_blank => true
            = f.input :omega_datum, :include_blank => true




      - unless @content.new_record?
        %fieldset
          %legend
            Dokumente
          %options


    
      .form-actions
        = f.button :submit
        - unless @content.new_record?
          = link_to close_project_content_type_content_path(@project, @content_type, @content), :class => 'btn btn-inverse pull-right' do
            %i.icon-unlock
        - else
          = link_to project_content_type_contents_path(@project, @content_type), :class => 'btn btn-inverse pull-right' do
            %i.icon-remove-sign
        
        
        
  
  .span3
    #sitemap.well.navi
      %ul.nested_branches
        = sitemap @branches.arrange(:order => :position) 