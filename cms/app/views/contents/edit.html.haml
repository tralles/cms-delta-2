.container
  .row
    .span3
      = render 'projects/menu'  
    
    .span9

      %h1
        Bearbeiten:
        %strong
          = @content_type.name
          %thin
            
            (
            = image_tag asset_path("flags/#{@locale}.png")
            )
      
      
      = render 'form'
