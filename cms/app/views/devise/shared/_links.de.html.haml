%ul.nav.nav-pills.nav-stacked
  - if controller_name != 'sessions'
    %li
      = link_to new_session_path(resource_name) do 
        %i.fa.fa-signin
        Anmelden

  
  - if devise_mapping.registerable? && controller_name != 'registrations'
    %li
      = link_to new_registration_path(resource_name) do 
        %i.fa.fa-leaf
        Account erstellen

  
  - if devise_mapping.recoverable? && controller_name != 'passwords'
    %li
      = link_to new_password_path(resource_name) do
        %i.fa.fa-umbrella
        Passwort vergessen

  
  - if devise_mapping.confirmable? && controller_name != 'confirmations'
    %li
      = link_to new_confirmation_path(resource_name) do
        %i.fa.fa-ok
        Account bestätigen
      
  
  
  - if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks'
    %li
      = link_to new_unlock_path(resource_name) do 
        %i.fa.fa-ambulance
        Account reaktivieren
      
  
  
  - if devise_mapping.omniauthable? 
    - resource_class.omniauth_providers.each do |provider| 
      %li= link_to "Anmeldung via #{provider.to_s.titleize}", omniauth_authorize_path(resource_name, provider)
